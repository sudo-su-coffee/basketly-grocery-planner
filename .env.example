# Basketly runs locally without environment variables.
# Add DATABASE_URL only if you later wire the optional server routes in src/app/api.
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require
